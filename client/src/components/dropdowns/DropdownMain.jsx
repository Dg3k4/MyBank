import React from 'react';
import "./dropdownMain.scss"

const DropdownMain = ({className}) => {
    return (
        <div className={`${className} dropdown`}>
            пока не нужно
        </div>
    );
};

export default DropdownMain;